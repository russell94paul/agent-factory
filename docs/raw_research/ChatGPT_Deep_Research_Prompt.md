# Deep Research Prompt — Agentic Futures Quant Platform on CELL//OS

> Fill the `[[BRACKETS]]`, then paste everything below the line into ChatGPT with Deep Research enabled. If it asks clarifying questions, the "Parameters" block should answer most of them.

---

**Task:** Produce a sourced, technical research report (target 8,000–12,000 words, tables and diagrams-in-text welcome) on how to build and operate an **agentic quantitative trading platform for CME/ICE futures, deployed through retail futures prop firms**, on top of my existing multi-agent operating system. Prioritize primary sources: peer-reviewed papers, exchange documentation, vendor/API docs, prop-firm rule pages, and engineering blogs from practitioners. Date-stamp every prop-firm rule and tool version you cite; flag anything you could not verify as current as of **September 2026**.

**My platform (context):** CELL//OS is a layered agent OS — Experience → Interface/Integration → Cognitive Core (Intent, Context, Planning, Execution, Memory, Learning, Evaluation, Evolution engines) → Coordination/Platform Services (identity, agent & capability registries, tool execution, knowledge broker) → Memory & Data (Neo4j, vector DB, Postgres, TimescaleDB, Snowflake, Iceberg) → Event/Telemetry (Kafka/NATS) → Runtime (LLM model gateway, secrets, config, policy engine/guardrails, Docker/WASM sandbox) → Foundation (infra, zero-trust, CI/CD, FinOps). Agents are "Operatives" grouped into Cells and Cell-Meshes; there is an adversarial evaluation engine ("Crucible": only the strong get promoted), an evolutionary discovery engine ("Blackbox"), and a Mission→Execute→Evidence→Evaluate→Learn→Mutate→Promote loop. A vertical "Domain Pane" slices every layer for a specific domain — here, futures quant.

**Parameters:**
- Prop firm(s): `[[e.g., Topstep, Apex, MyFundedFutures, Tradeify]]` — account size `[[$50K/$100K/$150K]]`
- Products: `[[e.g., ES/MES, NQ/MNQ, CL, GC, ZN]]`
- Holding period: `[[intraday minutes–hours | swing | mixed]]` (no HFT/sub-second — banned by firms)
- Connectivity available: `[[Tradovate / Rithmic / ProjectX / CQG / IBKR; data via Databento or dxFeed]]`
- Stack: `[[Python + Rust, cloud GPUs, monthly compute budget $X]]`
- Team: `[[solo / N people, skills]]`; timeline `[[X weeks]]`
- Objective: `[[harvest payouts fast | build a long-lived audited track record | both]]`

**Deliver these sections, in this order:**

1. **Data pipelines a systematic futures desk runs** — enumerate every pipeline (L2/L3 market data, bars incl. tick/volume/dollar/imbalance bars, roll calendars & continuous-contract construction methods, contract specs/margins/fees with change history, economic calendar with revisions, COT, options-on-futures vol surfaces, alternative data, own-execution exhaust, model/experiment artifacts, agent telemetry). For each: ingestion → validation → storage format/partitioning → serving → lineage, with 2–3 tool options (open-source first) and trade-offs. Cover point-in-time/bitemporal correctness and lookahead prevention explicitly.

2. **Model types, categorized by use-case** — statistical (state-space, regime-switching, cointegration, microstructure/Hawkes), ML (gradient boosting, meta-labeling, triple-barrier, sequence models incl. TFT/PatchTST, time-series foundation models with an honest assessment of whether they add alpha), RL (where it works and where it fails), evolutionary/genetic programming for signal discovery, ensembles/mixture-of-experts by regime, and the correct role of LLMs (research, code generation, news parsing — never in the order path). Cite evidence of live performance where it exists.

3. **The optimization and validation protocol, in strict order** — hypothesis pre-registration with a backtest budget; data hygiene gates; labeling; feature evaluation (IC/ICIR, MI, clustered feature importance, stability across folds/regimes); time-respecting CV (purged K-fold with embargo, CPCV, walk-forward, nested); multiple-testing correction (Deflated Sharpe Ratio, Probability of Backtest Overfitting, White's Reality Check/SPA); backtest statistics with confidence intervals and minimum track-record length; Monte Carlo (trade reshuffling, block bootstrap, parameter perturbation, regime-conditional) **including prop-firm survival simulation** (probability of passing an eval, probability of breaching trailing drawdown, expected time-to-breach, drawdown-constrained Kelly sizing, separate eval-stage vs funded-stage sizing); robustness (parameter-surface smoothness, cross-product transfer, crisis replays); multi-objective/Pareto selection (NSGA-II/III); forward/paper/shadow promotion criteria; portfolio-level dynamic weighting of many strategies (HRP, Bayesian model averaging, online bandits, regime-conditional weights) and how allocators themselves are validated; live decay detection (CUSUM on rolling Sharpe, PSI/KS drift, slippage drift, distance-to-breach) with an automatic de-risk ladder. Finish with one table: stage → metrics → gate thresholds → owning agent role.

4. **Modern architectures for feature engineering, ranking and selection** — formulaic/genetic alpha mining adapted to futures, LLM-proposed-then-auto-tested hypotheses, representation learning, regime-detection layer feeding mixture-of-experts, strategy tournaments (Elo/TrueSkill, survival analysis), strategy lifecycle states, and a "research firewall" (information barrier between hypothesis generation and evaluation to prevent gate-gaming).

5. **Backtesting, execution and connectivity stack** — compare NautilusTrader, hftbacktest, vectorbt, backtrader, Zipline-reloaded, QuantConnect LEAN and custom Rust engines on fidelity (L2 replay, queue position, latency, partial fills, rolls, fees, margin). Then broker/platform APIs (Tradovate, Rithmic R|API+, ProjectX, CQG, IBKR) and webhook bridges (TradersPost, PickMyTrade, Tradesyncer): capabilities, latency, reliability, and which prop firms run on which.

6. **Retail futures prop firms — connection and capital-scaling deep dive** — for each firm in my parameters: current drawdown model (EOD/intraday/static), daily loss limit, max contracts and scaling plan, consistency rule, news rules, automation & copy-trading policy (quote the rule text and date), concurrent-account cap, payout schedule/split/lifetime caps, fees/resets. Then: a step-by-step process to connect a compiled strategy compliantly (paper on the firm sim → parallel evals → funded sizing switch → own-account replication → payout-aware re-sizing → degradation-driven de-risking → recycling), and an allocation method for scaling capital across strategies, accounts and firms that treats accounts as containers, optimizes on strategy correlation, caps single-firm exposure, and conditions size on distance-to-breach. Include regulatory status (CFTC/NFA scrutiny of evaluation-based firms) as of the report date.

7. **Agentic architecture on CELL//OS** — which agent frameworks (LangGraph, CrewAI, AutoGen/AG2, OpenAI Agents SDK, Anthropic agent SDK, Temporal for durable workflows, MCP for tools) to wrap vs build natively; the Operative roster (data steward, feature scientist, hypothesis generator, model developer, backtest engineer, red-team reviewer, risk officer, prop-rule warden, execution trader, allocator/CIO, post-mortem analyst, SRE); mesh topology and, with evidence from multi-agent-systems research and practitioner reports, which team architectures are most reliable for trading (adversarial research/evaluation pairs, deterministic execution with veto-holding risk agent, human sign-off gates) and which fail; monitoring for degradation at agent, strategy and portfolio level; a week-by-week build plan for `[[X]]` weeks; and how to make every pipeline horizontally scalable (Ray/Dask for backtest and Monte Carlo fan-out, Kafka partitioning, lakehouse partitioning, stateless workers, FinOps guardrails) with rough capacity estimates.

8. **Gaps and warnings** — the most common ways platforms like this fail (overfitting via evolutionary search without a backtest budget, lookahead leaks, LLMs in the hot path, rule breaches from sizing, correlated multi-account blow-ups), and specific features I should add.

**Style:** dense and technical; tables for comparisons; numbered steps for protocols; distinguish *established practice* vs *promising but unproven* vs *hype*; every section ends with a short "CELL//OS mapping" (which layer/engine/Operative implements it). No generic risk disclaimers beyond one line. Cite as you go.
