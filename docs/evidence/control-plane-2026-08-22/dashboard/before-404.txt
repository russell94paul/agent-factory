[control] attempt_cap refused pipe_probe/trigger-run: stage 'trigger-run' has been dispatched 5 times; the cap is 5. Re-running it again will not fix it and each attempt costs real money. Supply an explicit override reason to buy one more attempt.
the ROUTE's own refusal:  HTTP 404
  body    {"error": "stage 'trigger-run' has been dispatched 5 times; the cap is 5. Re-running it again will not fix it and each attempt costs real money. Supply an explicit override reason to buy one more attempt."}

  !!  the route answers a control refusal with a status that is not 404  — HTTP 404 — 404 tells the browser the pipeline does not exist, so no client can distinguish a refusal from a typo
  !!  the refusal names the control in its body, so no client parses prose  — {"error": "stage 'trigger-run' has been dispatched 5 times; the cap is 5. Re-running it again will not fix it and each attempt costs real money. Supply an explicit override reason to buy one more attempt."}
  !!  the refusal was recognised as a cap refusal, not a generic error  — NO PROMPT — the regex in isAttemptCapRefusal did not match the real message
  ok  the first request carried no override
  !!  the second request carried the override to the wire  — 1 request(s)
  ok  the refusal was shown to the operator as an ERROR, not a success  — [error] Failed: stage 'trigger-run' has been dispatched 5 times; the
  !!  no success toast was raised before the override succeeded
  ok  cancelling sends NOTHING  — 1 request(s)
  !!  cancelling says nothing was retried
  ok  a whitespace-only reason is not an override  — 1 request(s)

FAILED: the dashboard did not behave as claimed.
