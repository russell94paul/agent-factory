# 02 — Artifact inventory for the CELL OS restructure
#
# GENERATED BY: the Phase 1 read-only audit, 2026-09-03, against agent-factory @ 827f871 (main).
# Nothing was moved, renamed or deleted to produce this file.
#
# ================================================================================================
# READ THIS BEFORE USING ANY FIELD
# ================================================================================================
#
# 1. ⛔ THIS IS NOT A REPLACEMENT FOR docs/_index/corpus_manifest.yaml.
#    That file exists, holds 168 records over 719 files, and is the corpus index. This file is
#    RESTRUCTURE-SCOPED: it records only what a migration decision depends on — destination,
#    duplication, tracked-ness, and the artifacts the manifest has never seen. Where the two
#    disagree about an interpretive field, THE MANIFEST WINS; where they disagree about a
#    mechanical field (bytes, hash), this file is newer and was measured today.
#
# 2. `current_status` uses the MISSION BRIEF's artifact vocabulary:
#       PROVEN | PARTIAL | SPECIFIED | PROPOSED | EXPERIMENTAL | SUPERSEDED | UNKNOWN
#    ⚠ It is NOT README.md §V.1's measurement vocabulary (PASS/FAIL/UNMEASURABLE/ERROR/NOT_RUN).
#    Those two vocabularies answer different questions and MUST NOT be merged. One describes an
#    artifact's maturity; the other describes whether an instrument could look.
#    PROVEN is used ONLY where runtime evidence, a passing test, or a completed rendered output
#    supports it. Nothing is PROVEN because a design document says so.
#
# 3. Record depth is tiered, stated per record as `record_tier`:
#      full    — every applicable field. Used for CELL OS material and load-bearing artifacts.
#      bundle  — one record over a homogeneous directory. The record names the count and the grain.
#    Tiering is a reading-cost decision, never a judgement about worth.
#
# 4. `content_hash` is the first 16 hex chars of the file's SHA-256, measured 2026-09-03.
#    Regenerate: python scratchpad/census.py census.tsv
#
# 5. `recommended_destination` is a PROPOSAL requiring approval. No path in this file has moved.
#    A destination of `NO MOVE` is a positive finding, not an omission — several are load-bearing.
#
# ================================================================================================

schema_version: 1

generated:
  at: 2026-09-03
  repo: agent-factory
  head: 827f871
  branch: main
  working_tree: dirty            # 18 modified + 51 untracked status entries
  generator: Phase 1 read-only audit; mechanical fields by scratchpad/census.py, interpretive fields authored by reading
  mechanical_fields_measured_by: |
    path/bytes/sha256 from the filesystem via hashlib; tracked-ness from `git ls-files`;
    archive contents from zipfile.namelist() with NO extraction.

census:
  files_in_working_tree: 1079
  tracked: 892
  untracked: 187
  excluded_from_census: [".git", ".data", ".sessions", ".worktrees", "__pycache__", ".pytest_cache"]
  regenerate_with: |
    python scratchpad/census.py census.tsv
    git ls-files | wc -l
  duplicate_content_groups: 54
  files_in_duplicate_groups: 141
  cross_directory_duplicate_groups: 50

# ------------------------------------------------------------------------------------------------
# PART A — The CELL OS corpus (24 artifacts)
# ------------------------------------------------------------------------------------------------
# ⭐ NOT ONE of these is described by a corpus_manifest.yaml record. Those under docs/raw_research/
#    PREFIX-match the bundle record AF-RAW-LOOSE, whose enumerated `bundle_members` list excludes
#    every one of them. Prefix coverage is not record coverage.
# 20 of 24 are untracked.

cell_os:

  - artifact:
      path: docs/raw_research/CELL_OS_Frontier_Audit_Research_Pack_2026-09-02.zip
      filename: CELL_OS_Frontier_Audit_Research_Pack_2026-09-02.zip
      record_tier: full
      type: archive/research_pack
      size: 15503568
      content_hash: 6cc48dc65fa3a922
      subject: >
        The frontier audit pack. Carries 01_CANONICAL_ONTOLOGY/ with the two most load-bearing
        terminology documents in the estate, plus deep-research prompts, experiment templates,
        five mermaid visuals, product-expansion hypotheses and 07_SOURCE_ARCHIVES/ (four nested
        ZIPs + Agent Factory Vision.txt).
      apparent_version: "2026-09-02"
      original_or_generated: original
      canonical_candidate: true
      canonical_candidate_note: >
        ⭐ 01_CANONICAL_ONTOLOGY/CELL_OS_Canonical_Terminology_vNext.md is the closest thing the
        estate has to a canonical CELL OS ontology, and KNOWN_TERMINOLOGY_COLLISIONS.md is the
        collision register. Both were read for this audit FROM INSIDE THE ARCHIVE; neither exists
        as a loose file, so nothing in the repository can grep them.
      current_status: SPECIFIED
      current_status_basis: >
        The ontology defines terms and marks its own open questions. It specifies vocabulary; it
        proves no implementation. Its own header says "design/research ontology".
      duplicates:
        - 07_SOURCE_ARCHIVES/raw_research.zip vs docs/raw_research.zip (name match; hash NOT compared — nested)
        - 07_SOURCE_ARCHIVES/CELL_OS_Project_Library_Master_2026-09-02.zip vs the loose copy (same)
        - 07_SOURCE_ARCHIVES/agentic_ai_concepts_pack.zip vs the loose copy (same)
        - 07_SOURCE_ARCHIVES/Agent Factory Vision.txt — a SEVENTH copy of the DC-01 file
      supersedes: null
      superseded_by: null
      recommended_destination: |
        ARCHIVE STAYS at docs/raw_research/ (immutable, untouched).
        SURFACE the two ontology files as READ-ONLY EXTRACTS:
          docs/architecture/canonical/terminology/CELL_OS_Canonical_Terminology_vNext.md
          docs/architecture/canonical/terminology/KNOWN_TERMINOLOGY_COLLISIONS.md
        each carrying a provenance header naming the archive and this hash.
      notes: >
        ⚠ Extraction over an existing path is forbidden by the brief. The proposal is a targeted
        two-file copy, not an unzip. The archive is not modified and not moved.

  - artifact:
      path: docs/raw_research/CELL_OS_Project_Library_Master_2026-09-02.zip
      record_tier: full
      type: archive/curated_library
      size: 9600123
      content_hash: 8452c116cfda2660
      subject: >
        361-entry curated re-packaging of the earlier packs, carrying its own
        00_START_HERE/{CLASSIFICATION_MAP, COVERAGE_SUMMARY, DISTRIBUTION_WARNING,
        DUPLICATE_HASH_GROUPS, FILE_MANIFEST_SHA256.csv, LIBRARY_ONLY_REFERENCES, PACK_CATALOG}.
      apparent_version: "2026-09-02"
      original_or_generated: generated       # a re-packaging of material already here
      canonical_candidate: false
      current_status: UNKNOWN
      current_status_basis: >
        Namelist read; contents not read. It carries its OWN duplicate-hash analysis and
        distribution warning, which must be reconciled against docs/_index/duplicate_clusters.md
        before either is trusted over the other.
      duplicates:
        - nested inside CELL_OS_Frontier_Audit_Research_Pack_2026-09-02.zip/07_SOURCE_ARCHIVES/
      recommended_destination: NO MOVE — stays at docs/raw_research/
      notes: >
        ⛔ Its DISTRIBUTION_WARNING.md must be read before ANY publication decision. Unread as of
        this audit. AF-RELEASE-GATE-01 is already BLOCKED; this may add to it.

  - artifact:
      path: docs/raw_research/CELL_OS_Product_Technical_Design_v0.1_Crossreference_Audit_v1.md
      record_tier: full
      type: synthesized_research/audit
      size: 32166
      content_hash: e0f49e31a46d219c
      subject: >
        A cross-reference audit of CELL_OS_Product_Technical_Design_v0.1 against newer work.
        Identifies the Cell-Mesh/federation collision at §§18, 72, 91, 102, 420 and recommends
        renaming the v0.1 "CELL Mesh: Federation" section to "OS-MESH Federation Fabric".
      apparent_version: v1
      original_or_generated: original
      canonical_candidate: false
      current_status: SPECIFIED
      duplicates: []
      supersedes: null
      superseded_by: null
      recommended_destination: docs/research/syntheses/CELL_OS_Product_Technical_Design_v0.1_Crossreference_Audit_v1.md
      notes: >
        ⛔ MISCLASSIFIED WHERE IT SITS. This is a SYNTHESIS — it reads sources and issues
        recommendations — filed under docs/raw_research/, which the brief and the repository both
        treat as immutable primary material. Leaving it there makes a derived opinion look like a
        source. This is the clearest single mis-shelving found in the audit.

  - artifact:
      path: docs/raw_research/CELL_OS_Van_City_World_Design_Brief_v1.md
      record_tier: full
      type: raw_research/design_brief
      size: 42710
      content_hash: null    # measured in census.tsv; see regenerate_with
      subject: >
        A world/metaphor design brief. Line 33 carries the plainest definition in the corpus:
        "A Cell Mesh is a team and coordination topology of Operatives or Cells." Agrees with the
        canonical ontology.
      apparent_version: v1
      original_or_generated: original
      canonical_candidate: false
      current_status: PROPOSED
      recommended_destination: NO MOVE — stays at docs/raw_research/
      notes: A design metaphor brief, not a specification. Its terminology agrees with canonical.

  - artifact:
      path: docs/design/CELL_OS_Product_Technical_Design_v0.1.docx
      record_tier: full
      type: architecture/technical_design
      size: 311438
      content_hash: 30131d258af53bff
      subject: The CELL OS v0.1 product technical design.
      apparent_version: v0.1
      original_or_generated: original
      canonical_candidate: true
      current_status: UNKNOWN
      current_status_basis: >
        ⛔ CONTENT NOT READ — no .docx reader was run against it in this pass. Everything known
        about it comes from its cross-reference audit, which is a second-hand source.
      duplicates: []
      superseded_by: >
        PARTIALLY, by CELL_OS_Canonical_Terminology_vNext.md on terminology only. The audit
        recommends RENAME+REFRAME of its federation section, not retirement of the document.
      recommended_destination: |
        NO MOVE for the .docx original.
        ADD a readable rendering at docs/raw_research/converted/CELL_OS_Product_Technical_Design_v0.1.md
        via the repository's existing scripts/docx_to_md.py.
      notes: >
        ⭐ HIGHEST-VALUE UNREAD ARTIFACT IN THE REPOSITORY. It is the named subject of a
        cross-reference audit that IS indexed, so the derived opinion is searchable and the source
        is not. Converting it is Batch 2's first act. Precedent: GAP-01 did exactly this for the
        two earlier .docx files at verified 100.1% character coverage.

  - artifact:
      path: docs/raw_research/CELL_OS_Master_Research_Design_Development_Operations_User_Guide_v0.2.docx
      record_tier: full
      type: raw_research/user_guide
      size: 66022
      content_hash: 724cb7db35cf0f18
      apparent_version: v0.2
      original_or_generated: original
      canonical_candidate: false
      current_status: UNKNOWN
      current_status_basis: CONTENT NOT READ. Extends GAP-01.
      recommended_destination: NO MOVE; add converted rendering alongside the above.

  - artifact:
      path: docs/raw_research/CELL_OS_Delivery_Backlog_v0.2.xlsx
      record_tier: full
      type: raw_research/delivery_plan
      size: 43536
      content_hash: 3b216ea69f789bdc
      apparent_version: v0.2
      original_or_generated: original
      canonical_candidate: false
      current_status: UNKNOWN
      current_status_basis: >
        ⛔ CONTENT NOT READ, and NOT-VISIBLE rather than merely unread: the repository has no .xlsx
        converter. scripts/docx_to_md.py does not apply. This stays unindexed after Phase 2 unless
        a converter is written.
      recommended_destination: NO MOVE
      notes: >
        This is the CELL OS delivery plan. It is the natural seed for docs/status/PROJECT_PROGRESS.yaml
        and cannot be used as one until it can be read. Flagged as Decision D-5.

  - artifact:
      path: docs/raw_research/CELL OS Design Master Brief.pdf
      record_tier: full
      type: raw_research/design_brief
      size: 248668
      content_hash: 83aa65edad7de580
      original_or_generated: original
      canonical_candidate: false
      current_status: UNKNOWN
      current_status_basis: CONTENT NOT READ. No PDF extractor configured. NOT-VISIBLE.
      recommended_destination: NO MOVE

  - artifact:
      path: docs/raw_research/cell-foundry-cross-domain-pipeline.html
      record_tier: full
      type: artifact/rendered_html
      size: 19497
      subject: A rendered cross-domain pipeline page for the Cell Foundry concept.
      original_or_generated: generated
      canonical_candidate: false
      current_status: EXPERIMENTAL
      recommended_destination: docs/artifacts/incoming/cell-foundry-cross-domain-pipeline.html
      notes: >
        ⭐ The ONLY one of the three files the mission brief names as expected incoming material
        that is actually present. Untracked; newest mtime in docs/raw_research/ (2026-09-03 13:05).
        It is a RENDERED ARTIFACT, not research, and is mis-shelved in raw_research/ on that basis.

  - artifact:
      path: docs/raw_research/CELL_OS_Architecture_Overview_Diagram.png
      record_tier: full
      type: design_asset/diagram
      size: 1282877
      content_hash: 26d4cdd6b34faa23
      original_or_generated: generated
      current_status: PROPOSED
      duplicates:
        - "docs/diagrams/CELL OS - Architecture Overview.png (1821873 B, hash 4cb356b5088ce98e) — DIFFERENT FILE, NOT a duplicate. Same subject, different bytes and size."
      recommended_destination: docs/architecture/diagrams/
      notes: >
        ⚠ Two architecture-overview diagrams exist with different content. Which is current is
        UNKNOWN. Do not consolidate them; do not pick one. Decision D-6.

  - artifact:
      paths:
        - docs/diagrams/CELL OS - Architecture Overview.png
        - docs/diagrams/CELL OS - Building your first OPC.png
        - docs/diagrams/CELL OS - Logo.png
      record_tier: bundle
      bundle_grain: one file per diagram; whole directory is untracked
      type: design_asset/diagram
      size: 4190913
      original_or_generated: generated
      current_status: PROPOSED
      recommended_destination: docs/architecture/diagrams/
      notes: >
        "OPC" in "Building your first OPC" is an unexpanded acronym appearing NOWHERE else in the
        repository. Most likely Operative Cell. UNRESOLVED — see 06_TERMINOLOGY_SUPERSESSION.md §5.
        The 4th file in this directory, tbAps.jpg, is unrelated and unnamed; excluded from this record.

  - artifact:
      path: docs/marketing/cell-os-launch-v1/
      record_tier: bundle
      bundle_grain: one launch-film package, 11 files (10 tracked, cell-os-deck.html untracked)
      type: product/launch_package
      size: 332307
      bundle_members:
        - README.md (8031 B, tracked)
        - claim-ledger.md (10644 B, tracked)      # ⭐ the status-label ledger
        - shooting-script.md (39969 B, tracked)
        - vo-script.md (7890 B, tracked)
        - motion-spec.md (9160 B, tracked)
        - sound-design.md (8134 B, tracked)
        - timing.md (6430 B, tracked)
        - variants.md (7360 B, tracked)
        - gen-prompts.md (11494 B, tracked)
        - previz.html (63126 B, tracked)
        - cell-os-deck.html (160069 B, UNTRACKED)
      original_or_generated: generated
      canonical_candidate: false
      current_status: PARTIAL
      current_status_basis: >
        PARTIAL, not PROVEN: rendered evidence exists at docs/evidence/cell-os-deck-2026-09-02/ and
        docs/evidence/cell-os-previz-2026-09-02/, so the pages render. Whether the CLAIMS render
        correctly is what claim-ledger.md exists to answer and was not re-verified in this pass.
      recommended_destination: NO MOVE — already correctly placed under docs/marketing/
      notes: >
        ⭐ THIS IS WHERE MOST OF THE CELL OS VOCABULARY ACTUALLY LIVES IN PROSE — 10 of the 18 files
        containing "CELL OS", 7 of 31 containing HyperMESH, 7 of 17 containing "Operative", 3 of 6
        containing CellBus. Marketing is ahead of both architecture and code. That asymmetry is the
        central finding of the terminology audit; see 06_TERMINOLOGY_SUPERSESSION.md §2.
        ⚠ claim-ledger.md already binds each claim to a status label. It is a PRIOR ART for the
        brief's PROVEN/SPECIFIED/PROPOSED vocabulary and the two must be reconciled, not stacked.

  - artifact:
      paths:
        - CELL_OS_NERVE_Design_Intelligence_MetaSkill_v1.zip
        - docs/design/CELL_OS_NERVE_Design_Intelligence_MetaSkill_v1.zip
      record_tier: full
      type: archive/skill_pack
      size: 27484
      content_hash: 06d98f16a2c80bff
      original_or_generated: original
      current_status: PROPOSED
      duplicates: "⚠ BYTE-IDENTICAL PAIR — confirmed by sha256sum, not by name or size."
      recommended_destination: |
        KEEP docs/design/CELL_OS_NERVE_Design_Intelligence_MetaSkill_v1.zip.
        The repo-root copy is a download landing in the wrong place; propose moving it to
        docs/_incoming/ rather than deleting it. NO DELETION in this plan.
      notes: The only NERVE-named artifact outside docs/raw_research/. "NERVE" appears 11 times in 2 files total.

  - artifact:
      paths:
        - docs/design/CELL_OS_Design_Build_Operate_Pack_v0.1.zip
        - docs/raw_research/CELL_OS_Design_Build_Operate_Pack_v0.1.zip
      record_tier: full
      type: archive/design_pack
      size: 354911
      content_hash: a7531f87de759f06
      apparent_version: v0.1
      original_or_generated: original
      current_status: PROPOSED
      duplicates: "⚠ BYTE-IDENTICAL PAIR — sha256sum confirmed. Only 3 entries in the archive."
      recommended_destination: |
        KEEP the docs/raw_research/ copy as the sealed original (matches the pack convention).
        LEAVE the docs/design/ copy in place until a human decides. NO DELETION.

  - artifact:
      paths:
        - docs/raw_research/CELL_OS_Design_Intelligence_Consolidation_v1.zip
        - docs/raw_research/CELL_OS_Ensemble_Forge_Skill_Pack_v0.1.zip
        - docs/raw_research/CELL_OS_Invention_Engineering_Skill_Pack_v0.1.zip
      record_tier: bundle
      bundle_grain: one sealed CELL OS skill/consolidation pack per file, all untracked
      type: archive/skill_pack
      size: 74363
      content_hashes: [e6a560d565ad63bf, 171390c1a0ca8d7c, 80d0a1749cbda0aa]
      original_or_generated: original
      current_status: UNKNOWN
      current_status_basis: Namelists read (14 / 17 / 24 entries). Contents not read.
      recommended_destination: NO MOVE

  - artifact:
      path: docs/design/claude-design/cell-os.global.yaml
      record_tier: full
      type: design_system/tokens
      size: 11420
      content_hash: 9d458276db882b98
      subject: A global design-token / canvas configuration for CELL OS.
      original_or_generated: generated
      current_status: EXPERIMENTAL
      recommended_destination: NO MOVE — docs/design/ is the right root for a design system.

  - artifact:
      path: boot-prompts/cell-os-deck-2026-09-03.md
      record_tier: full
      type: boot_prompt
      size: 12818
      content_hash: 06e4f069c8a7ad66
      original_or_generated: original
      current_status: PARTIAL
      recommended_destination: NO MOVE
      notes: >
        Covered only by the bundle record AF-BOOT-BUNDLE, whose enumerated members stop at
        2026-09-01. Five untracked boot prompts postdate the index.

  - artifact:
      path: docs/raw_research/Agent Factory- Switchboard View.html
      record_tier: full
      type: artifact/rendered_html
      size: 65072
      content_hash: 163d10455cfebafe
      original_or_generated: generated
      current_status: EXPERIMENTAL
      recommended_destination: docs/artifacts/incoming/
      notes: >
        Same mis-shelving as cell-foundry-cross-domain-pipeline.html — a rendered artifact filed as
        raw research. Note the OLD name ("Agent Factory") on a NEW file (2026-09-02), which is
        evidence for the terminology audit's finding that renaming is incomplete and ongoing.

# ------------------------------------------------------------------------------------------------
# PART B — Artifacts with a runtime dependency. THESE MUST NOT MOVE.
# ------------------------------------------------------------------------------------------------
# Each is read by factory/ at a hard-coded path. Moving any of them produces a SILENT failure —
# a gate that reports "nothing found" rather than an ImportError. That is the exact failure class
# this repository exists to prevent, and the restructure would be the thing introducing it.

runtime_coupled:

  - artifact:
      path: docs/findings.d/
      record_tier: bundle
      bundle_grain: one markdown file per finding, F20–F101, 35 files
      type: evidence/findings_ledger
      size: 176271
      original_or_generated: original
      current_status: PROVEN
      current_status_basis: >
        PROVEN — read as data at runtime by factory/findings.py:29 (FRAGMENTS), surfaced by
        factory/findings.by_lane(), and asserted over by tests/test_findings.py. Runtime evidence,
        not a document claim.
      recommended_destination: "⛔ NO MOVE — code interface"
      notes: >
        ⛔ F101 is currently missing three mandatory headings (BELIEVED / ACTUALLY / MEASURED BY)
        and names no lane. This is the sole cause of the 2 failing tests in the pre-migration
        baseline. PRE-EXISTING, unrelated to the restructure, and must not be fixed by Phase 2 —
        fixing it would remove the control that proves Phase 2 changed nothing.

  - artifact:
      paths: [docs/findings.md, docs/research/answers/, docs/artifacts/agent-factory.html, docs/evidence/]
      record_tier: bundle
      bundle_grain: four paths named literally in factory/ source
      type: mixed
      current_status: PROVEN
      current_status_basis: |
        Each is read by a named module at a named line:
          factory/findings.py:24      docs/findings.md
          factory/schedule.py:46      docs/artifacts/agent-factory.html  (ARTIFACT_REL)
          factory/readiness.py:647    docs/artifacts/agent-factory.html
          factory/readiness.py:1711   docs/research/answers/
          factory/readiness.py:1736   docs/evidence/render-pass-*.md
          factory/research_run.py:366 docs/research/answers/
          factory/synthesis.py:225    docs/research/answers/
      recommended_destination: "⛔ NO MOVE — code interface"
      regenerate_with: |
        grep -rn "docs/findings\|docs/research\|docs/artifacts\|docs/evidence" factory/*.py

# ------------------------------------------------------------------------------------------------
# PART C — Index and status artifacts
# ------------------------------------------------------------------------------------------------

indexes:

  - artifact:
      path: docs/_index/
      record_tier: bundle
      bundle_grain: 13 index files, all tracked
      type: index/corpus
      size: 845238
      bundle_members:
        - {file: corpus_manifest.yaml,             bytes: 275762, status: PROVEN,     note: "168 records / 719 files. STALE — 888 now under the same roots."}
        - {file: concept_index.yaml,               bytes: 196274, status: PROVEN,     note: "valid YAML"}
        - {file: document_catalog.md,              bytes: 48170,  status: PROVEN,     note: "prose companion to the manifest"}
        - {file: current_vs_proposed.md,           bytes: 39906,  status: PROVEN,     note: "⭐ the capability matrix — brief task 5's 'current vs proposed'. 10 parts."}
        - {file: contradictions.md,                bytes: 37818,  status: PROVEN,     note: "resolves nothing by design; presents evidence on each side"}
        - {file: high_leverage_concepts.md,        bytes: 38174,  status: PROVEN}
        - {file: research_gap_candidates.md,       bytes: 34415,  status: PROVEN,     note: "GAP-01 is the .docx conversion gap this audit re-confirms"}
        - {file: repo_snapshot.md,                 bytes: 27837,  status: PROVEN,     note: "covers factory/*.py, which the manifest excludes"}
        - {file: SUPPLEMENT_README.md,             bytes: 24354,  status: PROVEN}
        - {file: supersession_candidates.md,       bytes: 20288,  status: PROVEN,     note: "advisory only; marks nothing obsolete"}
        - {file: duplicate_clusters.md,            bytes: 18852,  status: PARTIAL,    note: "DC-01..DC-13 all re-confirmed by hash today; 3 new clusters uncovered"}
        - {file: agent_army_wave0_supplement.md,   bytes: 52800,  status: PROVEN}
        - {file: agent_platform_delta_synthesis.md,bytes: 30588,  status: PROVEN}
      current_status: PARTIAL
      current_status_basis: >
        PARTIAL for the set: every file is valid and internally sound, and the corpus it describes
        has grown 23.5% since it was written. Its interpretation is not wrong; its population is
        incomplete.
      recommended_destination: NO MOVE — docs/_index/ is already the right home
      notes: >
        ⛔ DO NOT CREATE COMPETING INDEXES. The mission brief names seven index artifacts; six of
        the seven exist here. The correct action is a DELTA PASS, not a rebuild.
        MISSING and proposed: research_registry.yaml, research_status.md.

  - artifact:
      path: docs/status/
      record_tier: full
      type: status/progress_tracker
      current_status: PROPOSED
      recommended_destination: docs/status/PROJECT_PROGRESS.yaml + PROJECT_PROGRESS.md
      notes: >
        ⛔ DOES NOT EXIST. No progress tracker exists anywhere in the repository. Nearest existing
        artifacts, all inspected: .agent-platform/bootstrap/schemas/platform-progress.schema.json
        (a SCHEMA from the stranger's pack, not an instrument), .claude/commands/af-status.md (a
        runtime status command over factory.autonomy, live state not project state),
        docs/board/tickets.json (a board artifact), and factory/roadmap.py (derives a roadmap from
        gate verdicts and "has no task list, by design"). None is a project progress tracker.
        See 05_MIGRATION_PLAN.md Batch 5 and Decision D-7.

  - artifact:
      path: docs/_incoming/
      record_tier: full
      type: intake
      current_status: PROPOSED
      recommended_destination: docs/_incoming/
      notes: >
        ⛔ DOES NOT EXIST. Measured: `ls -d docs/_incoming` → No such file or directory.
        Inbound material currently lands directly in docs/raw_research/ (and, for three CELL OS
        files, in docs/design/ and the repo root). This is the single structural gap that most
        directly explains the mis-shelvings recorded in Part A.

# ------------------------------------------------------------------------------------------------
# PART D — Duplicate clusters not covered by docs/_index/duplicate_clusters.md
# ------------------------------------------------------------------------------------------------
# ⛔ NOTHING IN THIS SECTION IS PROPOSED FOR DELETION. The brief forbids discarding a duplicate
#    before contents are compared; DC-08 in the existing register shows why — there, byte-identity
#    between an artifact and its fallback IS the proof the fallback is not stale.

new_duplicate_clusters:

  - cluster:
      id: DC-14 (proposed)
      shape: DIRECT DUPLICATE — a whole directory, 28 of 28 files
      a: docs/combined-execution-research-v2-2026-09-02/
      b: docs/raw_research/agent-factory-combined-execution-research-pack-v2-2026-09-02/
      evidence: every one of the 28 files hash-matches its counterpart (census.tsv.dups.txt)
      third_copy: >
        13 of the 28 also appear a THIRD time in
        docs/raw_research/agent-factory-deadline-execution-pack-2026-09-02/ — the v1 pack that v2
        supersedes. Those 13 are the files v2 did not change.
      recommendation: >
        KEEP the docs/raw_research/ extraction as the pack (matches convention).
        The docs/ copy is an out-of-convention second extraction of the same ZIP, all untracked.
        Propose retiring it to docs/archive/ AFTER a human confirms nothing links to it.
        DO NOT DELETE.
      status: PROPOSED

  - cluster:
      id: DC-15 (proposed)
      shape: DIRECT DUPLICATE — two ZIP pairs
      pairs:
        - {hash: 06d98f16a2c80bff, size: 27484,  paths: ["CELL_OS_NERVE_Design_Intelligence_MetaSkill_v1.zip", "docs/design/CELL_OS_NERVE_Design_Intelligence_MetaSkill_v1.zip"]}
        - {hash: a7531f87de759f06, size: 354911, paths: ["docs/design/CELL_OS_Design_Build_Operate_Pack_v0.1.zip", "docs/raw_research/CELL_OS_Design_Build_Operate_Pack_v0.1.zip"]}
      recommendation: Keep both copies until a human decides. See Part A records.
      status: PROPOSED

  - cluster:
      id: DC-16 (proposed)
      shape: DIRECT DUPLICATE — artifact vs its deadline fallback
      pairs:
        - {hash: b6d09ee9a00c8783, paths: ["docs/artifacts/client-review-navira.html", "docs/evidence/deadline-2026-09-02/fallback/client-review-navira.html"]}
        - {hash: 98fe104675dcf5eb, paths: ["docs/evidence/client-review-readiness-2026-09-01/render-check-client-review.json", "docs/evidence/deadline-2026-09-02/fallback/render-check-client-review.json"]}
      recommendation: >
        ⚠ PROBABLY NOT A DEFECT. DC-08 records the identical shape and concludes "the identity IS
        the proof" — a fallback that differed from the artifact would be a stale fallback.
        NO ACTION. Recorded so a future dedupe pass does not "fix" it.
        ⛔ Note docs/artifacts/client-review-navira.html is one of the 18 currently-MODIFIED files,
        so this pair may already have diverged in the working tree.
      status: PROPOSED

# ------------------------------------------------------------------------------------------------
# PART E — Everything else, at bundle grain
# ------------------------------------------------------------------------------------------------
# These roots are already correctly placed. Recorded for completeness and to state explicitly that
# the plan does not touch them.

unchanged_roots:
  - {path: "factory/",                 files: 68,  bytes: 1200518,  type: source_code,  status: PROVEN,  destination: "NO MOVE — see 01 §4.1"}
  - {path: "tests/",                   files: 51,  bytes: 631663,   type: source_code,  status: PROVEN,  destination: "NO MOVE",  note: "1016 pass / 2 fail baseline"}
  - {path: "scripts/",                 files: 43,  bytes: 572913,   type: source_code,  status: PROVEN,  destination: "NO MOVE"}
  - {path: "evaluator_service/",       files: 5,   bytes: 22037,    type: source_code,  status: PROVEN,  destination: "NO MOVE — separate identity on purpose"}
  - {path: "evals/",                   files: 2,   bytes: 8303,     type: eval_corpus,  status: PROVEN,  destination: "NO MOVE",  note: "hash-verified on load via MANIFEST.sha256"}
  - {path: "blueprints/",              files: 2,   bytes: 11941,    type: config,       status: PROVEN,  destination: "NO MOVE"}
  - {path: "missions/",                files: 13,  bytes: 160512,   type: mission_spec, status: PROVEN,  destination: "NO MOVE"}
  - {path: "docs/raw_research/",       files: 326, bytes: 34399490, type: raw_research, status: UNKNOWN, destination: "⛔ IMMUTABLE — brief forbids alteration. 2 mis-shelved artifacts flagged in Part A; the proposal is to COPY, never to move or edit."}
  - {path: "docs/evidence/",           files: 160, bytes: 68828474, type: evidence,     status: PROVEN,  destination: "⛔ NO MOVE — runtime-coupled (readiness.py:1736)"}
  - {path: ".agent-platform/",         files: 113, bytes: 604231,   type: proposal,     status: PROPOSED, destination: "NO MOVE", note: "⚠ README §VII: a proposal from a stranger. RECONCILIATION.md and PACK_CONFORMANCE.md were written here from measurement and OUTRANK it."}
  - {path: "docs/protocol/",           files: 22,  bytes: 79976,    type: spec,         status: PARTIAL, destination: "NO MOVE", note: "DC-11 records protocol-built-vs-designed as contradictory-adjacent"}
  - {path: "docs/specs/",              files: 9,   bytes: 143243,   type: spec,         status: PARTIAL, destination: "NO MOVE", note: "3 files referenced by name from factory/ docstrings"}
  - {path: "docs/agent-army/",         files: 5,   bytes: 37063,    type: index/measured, status: PROVEN, destination: "NO MOVE", note: "⭐ CURRENT_STATE.md is code-measured and OUTRANKS corpus_manifest.yaml by that file's own preamble"}
  - {path: "docs/release-gate/",       files: 5,   bytes: 189117,   type: governance,   status: PROVEN,  destination: "⛔ NO MOVE, and DELIBERATELY UNTRACKED. AF-RELEASE-GATE-01 is BLOCKED pending human authority."}
  - {path: "boot-prompts/",            files: 26,  bytes: 323166,   type: boot_prompt,  status: PROVEN,  destination: "NO MOVE", note: "router is boot-prompts/README.md; 5 untracked postdate the index"}
  - {path: "docs/artifacts/",          files: 8,   bytes: 659454,   type: artifact,     status: PROVEN,  destination: "⛔ NO MOVE — runtime-coupled (schedule.py:46, readiness.py:647)"}
  - {path: "docs/research/",           files: 59,  bytes: 3771158,  type: research,     status: PROVEN,  destination: "⛔ NO MOVE for answers/ — runtime-coupled. See 07_RESEARCH_STATUS_AUDIT.md"}
  - {path: ".claude/commands/",        files: 6,   bytes: 4290,     type: command,      status: PROVEN,  destination: "NO MOVE — /cell-* commands are ADDED beside them"}

# ------------------------------------------------------------------------------------------------
# Coverage statement
# ------------------------------------------------------------------------------------------------

coverage:
  files_in_working_tree: 1079
  covered_by_a_record_here: 1079
  coverage_basis: >
    Every file is covered either by a `full` record, as a named member of a `bundle` record, or by
    an `unchanged_roots` row naming its root. No file was omitted for being judged unimportant.
  not_read:
    - {what: "docs/design/CELL_OS_Product_Technical_Design_v0.1.docx", bytes: 311438, verdict: NOT-VISIBLE, why: "no .docx reader run; converter EXISTS (scripts/docx_to_md.py)"}
    - {what: "docs/raw_research/CELL_OS_Master_..._User_Guide_v0.2.docx", bytes: 66022, verdict: NOT-VISIBLE, why: "same"}
    - {what: "docs/raw_research/CELL_OS_Delivery_Backlog_v0.2.xlsx", bytes: 43536, verdict: NOT-VISIBLE, why: "⛔ NO .xlsx converter exists in this repository"}
    - {what: "docs/raw_research/CELL OS Design Master Brief.pdf", bytes: 248668, verdict: NOT-VISIBLE, why: "⛔ NO PDF extractor configured"}
    - {what: "interiors of 32 ZIP archives", bytes: 34000000, verdict: NOT-VISIBLE, why: "namelists read; only the 2 canonical-ontology files were read from inside an archive"}
    - {what: "~/repos/agent-army-research (155 md files)", verdict: OUT-OF-SCOPE, why: "a separate repository; see 01_REPOSITORY_AUDIT.md §7"}
  what_this_inventory_deliberately_does_not_do:
    - It does not delete, merge or retire any duplicate. Every copy stays.
    - It does not promote any incoming artifact to canonical status.
    - It does not resolve a contradiction; docs/_index/contradictions.md owns that.
    - It does not restate the 168 records already in docs/_index/corpus_manifest.yaml.
    - It does not mark anything PROVEN on the strength of a design document.
