# Post-Deadline Research / Measurement Order

Do not run this before the meeting unless a live blocker makes one item essential.

## First: measurements and decisions, not research

1. **RB-00C / GAP-09 — one real agent run**, if the Marketing vertical slice did not already close it.
2. **RB-00D — settle tenancy questions**, then **RB-00B — score a second connector**.
3. **RB-00E — enumerate knowledge stores**.
4. **RB-00F — disposition every research absorption row**.

These reduce more uncertainty than another generic report.

## Wave 1 — foundations

Priority order recommended for this product:

1. **RB-02 — trace/trajectory standards.** Structured trajectories unlock replay, credit assignment, meta-tool extraction and better evaluation.
2. **RB-01 — organisation-oriented MAS prior art.** Determine what Moise+/JaCaMo/IMACS already provide for the actual workload; do not research novelty for its own sake.
3. **RB-17 — evidence vocabulary crosswalk.** Reconcile Observation/Claim/Evidence/Knowledge terminology before schema expansion.
4. **RB-06 — configuration parameter taxonomy.** Useful once executions can generate fitness observations.
5. **RB-08 + RB-05 — threat model and compensation/rollback**, with scope boundaries preserved.
6. **RB-20 — interoperability standards.** Valuable before hard-coding internal protocols.
7. Remaining Wave-1 missions per the corpus dependency graph.

## Wave 2 — only after real outcomes exist

- topology-task fit;
- health metric validity;
- error correlation;
- organizational eval protocol;
- credit assignment;
- task packaging standards.

## Evolution / organization search gate

Do not build an Evolution Chamber or quality-diversity archive until:

- multiple real runs exist;
- at least two model families/config bindings have been compared;
- revalidation cadence is part of the version/config contract;
- the evaluator cannot be rewritten by the actor being scored.

## `.agent-platform` delta additions

After the first real runs and immediate measurement items:

- **RB-21 — Execution surface routing** if it was not already absorbed by the deadline implementation. Prefer a measured routing experiment over architecture prose.
- **RB-22 — Evidence-backed capability record** after at least two real workload/config observations. Extend the existing registry; use standard discovery semantics where possible.
- **RB-24 — Synthesis Inbox** as a small operator experiment if parallel-session/research handoff loss remains material.
- **RB-23 — Venture/customer/portfolio loop** only after one certified team proves the Factory can repeatedly deliver.

Do not create a second research queue from `.agent-platform/bootstrap/schemas/research-job.schema.json`; merge useful fields into the existing research estate.
