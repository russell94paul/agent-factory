**Seven layers over one substrate**

## Click any layer to open it

Each layer is a governance boundary, not just a box. Open one to see the services inside it, what enters, what leaves, its operating-system ancestor, and how mature it actually is. Layers are stacked as the reference diagram draws them — experience at the top, runtime at the bottom, requests travelling down and results travelling up.

**External inputs →**

- Users & Teams
- Mission Briefs
- Data Sources
- Systems & APIs
- Files & Docs
- Devices & IoT

**→ External outputs**

- Analytics & Reports
- Automations
- Notifications
- Open APIs
- Data Exports
- Client Applications

**1Experience Layer**Interact & Observe — the only layer a human touches7 SURFACES · PROTOTYPE

#### What happens at this layer

Humans express intent and observe consequences. **No business logic lives here.** Every surface is a projection of state the runtime already holds — which is why the dense operator view and the spatial Avatar World can coexist without drifting apart.

#### Services

**Mission Control**

Command Center

**Briefing Room**

Intent & Context

**Agent Workbench**

Build & Configure

**Org Studio**

Design Organizations

**Org Steward**

Performance & Health

**Eval Dashboard**

Performance & Health

**Observability Console**

Telemetry & Tracing

**Receives**

User goals and mission briefs from outside; `results · artifacts` from the Interface layer below.

**Emits**

`analytics · reports` outward; API requests downward.

**OS ancestor**

The shell and window manager. It renders; it does not decide.

**Maturity**

PROTOTYPE A switchboard projection and a rendered client-review surface exist and are render-confirmed. The rest is designed.

results · artifacts

**2Interface & Integration**Connect & Integrate — one door in, one door out4 SERVICES · PLANNED

#### What happens at this layer

Every request that enters or leaves CELL OS crosses this boundary, which is what makes authentication, rate limiting and audit tractable. A connector that bypasses it becomes a second, untested control plane — the failure mode the design explicitly forbids.

#### Services

**API Gateway**

Single Entry

**Webhooks**

Event Ingest

**Connector Framework**

Standardized Connectors

**Client SDKs**

APIs & Libraries

**Receives**

Calls from the Experience layer, and inbound events from external systems.

**Emits**

`API requests` into the Cognitive Core; `results · artifacts · responses` back out.

**OS ancestor**

The syscall interface and device layer.

**Maturity**

PLANNED A connector contract with twelve calibrated assertions exists and scores 12/12 — but **replayed against one recorded corpus**, and 48 connectors have never been scored. Sensitivity is not coverage.

API requests

**3Cognitive Core**Think & Decide — the only layer permitted to reason8 ENGINES · PLANNED

#### What happens at this layer

Intent becomes an organization. The Intent Engine reads a goal, the Context Engine compiles only the knowledge that goal authorizes, the Planning Engine decomposes it, and the Execution Engine orchestrates. **Reasoning is confined here on purpose** — no other layer is allowed to improvise, so the layers below stay deterministic and auditable.

#### Services

**Intent Engine**

Understand Goals

**Context Engine**

Compile Context

**Planning Engine**

Plan & Decompose

**Execution Engine**

Orchestrate

**Memory Engine**

Episodic & Semantic

**Learning Engine**

Adapt & Improve

**Evaluation Engine**

Score & Validate

**Evolution Engine**

Mutate & Evolve

**Receives**

`API requests` carrying a mission intent and its authority boundary.

**Emits**

`plans · tasks · tool calls` downward; `decisions · insights · models` outward.

**OS ancestor**

The compiler plus the scheduler — Mission intent → Org-IR → linked Cell Image.

**Maturity**

PLANNED for the compiler — the Org-IR schema is marked *seed only* and the category itself is contested in the source corpus. FRONTIER for the Evolution Engine.

plans · tasks · tool calls

**4Coordination & Platform Services**Coordinate & Enable — where a request is authorised or refused8 SERVICES · PROTOTYPE

#### What happens at this layer

This is the Kernel's service plane and the most security-critical layer in the stack. An Operative never holds a credential; it asks. Identity, capability scope, parameter bounds, budget, policy, evidence prerequisites and — where required — a human are all checked here before any side effect reaches the outside world.

#### Services

**Identity & Access**

AuthN / AuthZ

**Org Graph Service**

Relationships

**Agent Registry**

Agents & Roles

**Capability Registry**

Skills & Tools

**Tool Execution**

Run Tools

**Knowledge Broker**

Route Knowledge

**Communication Service**

Message & Notify

**Notification Service**

Alerts & Updates

**Receives**

`plans · tasks · tool calls` from the Cognitive Core.

**Emits**

`actions · responses`; `state · memory` writes downward.

**OS ancestor**

Kernel services and drivers. A Capability Call is this stack's `syscall`.

**Maturity**

PROTOTYPE 30 readiness gates and four typed evidence classes are implemented, and the task layer raises `EvidenceRequired`. ⚠ But the known-failure preflight is **WARN-ONLY — it refuses nothing.**

state · memory

**5Memory & Data**Store & Retrieve — authority and projection, kept apart7 STORES · PLANNED

#### What happens at this layer

HyperMESH lands here. The rule that matters is the separation the design insists on: **authoritative evidence is never confused with a derived retrieval projection.** A vector index is a convenience for finding things; it is never the record of what happened. Provenance survives both.

#### Services

**Org Graph DB**

Neo4j

**Vector DB**

Pinecone

**Document Store**

MongoDB

**Relational DB**

PostgreSQL

**Time Series DB**

TimescaleDB

**Data Warehouse**

Snowflake

**Lakehouse**

Iceberg

**Receives**

`state · memory` writes from the coordination layer.

**Emits**

`structured data · knowledge` outward; `events · state deltas` downward.

**OS ancestor**

The filesystem, plus the page cache. Context VM is the paging discipline over it.

**Maturity**

PLANNED HyperMESH is the single most contested concept in the source corpus — designed and specified, not built.

events · state deltas

**6Event & Telemetry**Emit & Stream — append-only, and the reason replay is possible6 SERVICES · PROTOTYPE

#### What happens at this layer

Everything that happened, as typed append-only streams. This is not logging as an afterthought — **it is the substrate Replay, the Cell Profiler and every assurance receipt are built on.** If an event was not emitted here, it did not happen as far as the platform can later prove.

#### Services

**Event Bus**

Kafka / NATS

**Mission Events**

Lifecycle & verdicts

**Agent Events**

Operative activity

**Telemetry Collector**

Gather

**Telemetry Stream**

Distribute

**Alerts**

Thresholds & anomalies

**Receives**

Events from every layer above, and from the runtime below.

**Emits**

`events · telemetry` outward; `events · telemetry · alerts` to the runtime layer.

**OS ancestor**

The kernel log and audit subsystem.

**Maturity**

PROTOTYPE An append-only event ledger exists and holds **61 events** — of which 7 are agent returns, and **all 7 carry ****`dry_run=True`**.

events · telemetry · alerts

**7Runtime & Foundation**Run & Operate — where code actually executes, under isolation7 SERVICES · PLANNED

#### What happens at this layer

Real execution, real secrets, real blast radius. The Model Gateway decides which model answers; the Sandbox Runtime decides what that answer is allowed to touch; the Policy Engine is the hard stop. Everything above this layer is, ultimately, a proposal.

#### Services

**Observability Stack**

Logs, Metrics, Traces

**Model Gateway**

LLM Routing

**Secrets Manager**

Keys & Tokens

**Config Service**

Feature Flags

**Policy Engine**

Guardrails

**Sandbox Runtime**

Docker / WASM

**Backup & DR**

Resilience

**Receives**

Authorised actions, and the event stream from layer 6.

**Emits**

`alerts · notifications · system health`.

**OS ancestor**

The kernel proper, plus hardware abstraction.

**Maturity**

PLANNED with a PROTOTYPE isolation ladder. ⚠ The design itself warns that an ephemeral zero-copy clone is *“a compromised oracle at exactly the layer our evidence rule exists to protect”*.

**Foundation — the substrate**

Infrastructure · Cloud / On-PremSecurity & Identity · Zero TrustNetwork · Private / EncryptedMonitoring · Health & PerformanceCost Management · FinOpsCompliance · Audit & PolicyCI/CD · PipelinesGovernance · Data & AI

**The mechanism worth understanding**

## What a capability call actually passes through

This is the single most important control path in the platform. An Operative cannot merge a pull request, deploy, spend money or email a customer directly — it requests, and the Kernel decides. Seven checks, and any one of them can stop it.

Operative request   repo.merge\_pull\_request(...)         │         ▼ CELL KERNEL    identity?              is this Operative who it claims to be    capability scope?      is this operation in its grant    parameter boundary?    is *this repo*, *this branch* in bounds    namespace?             right environment, right tenant    budget?                tokens, time, spend remaining    policy?                hard organizational rules    evidence prerequisite? has it proved what it must first    human authorization?   a person must decide         │         ├──▶ DENY  /  REQUEST GATE         │         ▼ Capability Driver ──▶ external system ──▶ result ──▶ audit / evidence

**Why the last line matters.** The call does not end at the result. It ends at an audit record and typed evidence — which is what allows a mission to later be certified rather than merely reported.

### 🔥 The Crucible FRONTIER

**Survival is the benchmark.** Ideas, agents, workflows and whole organizations are tested in adversarial environments. Only the strong get promoted.

Concretely: a configuration does not graduate because it worked once. It graduates because it survived a deliberate attempt to break it, and the attempt is on record.

### ⬢ The Blackbox FRONTIER

**Put the problem in. See what evolves.** Unknown in, useful out.

The counterpart to the Crucible: where the Crucible tries to kill a known configuration, the Blackbox searches for one nobody specified. Both are research surfaces, and both are gated behind the same unlock as the optimizer.

**CELL//OS Command Deck** · built from the Architecture Overview and Build-Your-First-OPC diagrams, Product Technical Design v0.1, and Master Research / Design / Development / Operations User Guide v0.2.
Inter-layer flow labels follow the source diagram, with one correction: the label between layers 5 and 6 is rendered as **events · state deltas** where the source repeats *plans · tasks · tool calls*.
Maturity chips follow the product's own legend (Technical Design §1.3). Pricing figures are **ASSUMED** models, never measurements. The prototype tab runs locally and calls nothing.