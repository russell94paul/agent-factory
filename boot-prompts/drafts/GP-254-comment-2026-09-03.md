# GP-254 Jira comment — POSTED 2026-09-03, kept for the record

**Ticket verified:** GP-254 "Lectric eBikes Integration — Agency Test", project GEP, labels
`navira-roadmap` / `phase-1c`, assignee Paul Russell, **status Done**, last updated 2026-07-20.
https://analyticlabsdc.atlassian.net/browse/GP-254

**Status: POSTED.** The first attempt was blocked by the Claude Code auto-mode classifier; a
retry in the same session succeeded (comment id `36274`). The block was transient, not a
capability limit — worth retrying rather than assuming the MCP is unavailable. Delivery work was
split out to **GP-330** and the posted version references it; otherwise the body below is what
went up.

**No wiki links in this comment** — the wiki is private.

---

**Read-only status audit of the Lectric agency feed — 2026-09-03. Nothing deployed, nothing changed.**

Triggered by a client question about how to hand Lectric sales data to Justin. This ticket is closed as Done but the feed is still running and the delivery decision is open, so recording the measured state.

## The dataset — TEST only

`TEST_DG1_GEP.WAREHOUSE.SALES_FCT_ORDERLINE` where `ENTITY_CODE='LECTRIC'`:

| metric | value |
|---|---|
| order lines | 5,310 |
| orders | 5,008 |
| products (ASINs) | 29 |
| units | 5,713 |
| gross USD | $2,822,972 |
| date range | 2024-05-31 → 2026-09-03 |

27 months of real trading data — **not** a two-month connection test. The bounded `min_date` misled us initially: there are two loads. A one-off backfill covering 2024-05 onward (measured at 4,464 rows on 2026-06-19 in this ticket's design doc) plus the scheduled connector appending ~840 rows since July. The connector **extends** an existing dataset rather than having created it.

## TEST vs PROD parity

Run as `R3_CARTOGRAPHY_RO` (TEST) and `PROD_DG1_CORE_ADMIN`/ACCOUNTADMIN (PROD).

- **The only column difference is `ENTITY_CODE`** — TEST 118 columns, PROD 117. PROD-only columns: none. Shared: 117.
- **PROD has no `ENTITY_CODE`** in either `WAREHOUSE` or `WAREHOUSE_SOURCE`, so PROD still runs the pre-agency single-entity fact. Expected — no promotion past TEST.
- **`AMAZON_LECTRIC` schema absent from PROD** (0 objects). Confirmed absent, not empty and not a grant issue.
- Navira ties across environments: TEST `NAVIRA` 3,551,296 lines / $131,714,461 vs PROD 3,548,562 / $131,632,262 across the top 12 companies — 0.06% apart, consistent with the untruncated remainder.

Consequence for any consumer: `WHERE ENTITY_CODE='LECTRIC'` against PROD **errors** (`000904` compilation), it does not return zero rows.

## The API connector — live and healthy

TEST Cosmos (`aldctestcsdb1c01`), template `e7eaf791-88cc-5b88-bad6-9883a651cb19`:

- `status=active`, `GET_FLAT_FILE_ALL_ORDERS_DATA_BY_LAST_UPDATE_GENERAL`, **Amazon US only**
- live `min_date=2026-07-01`; git says `2024-06-01`, preserved as `_orig_min_date` — bounded at activation by the deploy script's backfill guard
- **67 partitions, every one with `run_count > 0`**, 65 carrying result hashes. Genuine execution, daily ~10:0x UTC.
- **No agent doc exists** for account `da8904db` — it is being picked by the general fleet, not a Lectric-scoped executor. Mechanism not proven; only that execution happens without one.
- **No field selection anywhere in the live doc** (`options` = `category`, `filter` only). It pulls the full flat file, so any column Amazon emits already lands. Adding warehouse columns is SQL-only — no template change, no Amazon backfill.
- Confirmed landing during the session: 5,304 → 5,310 lines and raw 1,441 → 1,454 rows across queries minutes apart.

## Column census for a client-facing share

On the 3,067,775 real sales rows (`LINE_STATUS <> 'Unknown'`): **32 of 78 numeric columns are structurally inert** — never non-zero. Includes the entire `*_SUBSIDIARY` family (9), all `REVENUE_*` (12), `SALES_REVENUE_*` (3), `SALES_MARKDOWNS_*` (4), `NOSALE_ADVERTISING_FEE_*` (2). Sharing any of them would assert "$0.00" as a fact about the client's business. A sales-only share view of ~24 live columns has been specified but **not built**.

## Related: the `LINE_STATUS='Unknown'` bucket (GP-311)

Investigated because it is 13.56% of the PROD fact. **Expected and correct** — 481,067 synthetic cost-allocation rows parking $446,188.60 of no-sale advertising spend ($441,724.79 product + $4,463.81 brand), with `MARGIN_NET_CONSOLIDATED` at exactly −$446,188.60. Ties to the cent. The `ORDER_ID`-nulled / `ORDER_KEY`-populated guard is intact on all 481,067 rows. No action needed; noted here because a sales-only share must exclude them or they become 13.56% pure noise.

## Validation layer — stated plainly

**Query layer only.** Snowflake SQL and Cosmos document reads. **No rendered check, no PBI model opened, no dashboard loaded.** Any claim here about what a consumer *sees* is unverified.

## Open / next

1. **Client decision (blocking):** one-off extract then switch the connector off, or keep collecting and stand up a Snowflake share ALDC → Navira. Email drafted, not sent.
2. **A share requires PROD promotion first.** The Lectric objects and `ENTITY_CODE` are TEST-only, so the one-off path is available today and the share path is not. The two options are not equally ready.
3. **Decide whether to leave the connector running.** It has run unattended ~2 months. Deactivation is a template flip (`--apply` without `--activate`), not an agent kill.
4. **COGS for Lectric remains absent** — GP-297 is the ticket for that.
5. **`BY_LAST_UPDATE` backfill risk:** if history is ever re-pulled for new columns, partitions walk *update* dates, so quiet 2024 orders may never re-emit. The `BY_ORDER_DATE` variant would be needed. Unverified against `amazon_sellercentral_v1`.

## Pointers

- Eclipse connection + template: `clients` repo, branch `feature/paulrussell/lectric-scheduled-connector` (`GEP/eclipse/connections/lectric_amazon_seller_central.json`, `GEP/eclipse/templates/amazon_lectric/all_orders_report.json`). **Not merged to `GEP/development`.**
- Superseded 4-marketplace variant (US/CA/MX/BR): branch `feature/paulrussell/gp-254/lectric-agency-onboarding`. The live design is US-only.
- Deploy tool: `aldc-launchpad/warehouse_ops/_deploy_lectric_cosmos_test.py` (plan-then-apply, inactive-first, `--activate` gated behind `--min-date`).
- Warehouse SQL: `clients/GEP/snowflake/warehouse/sales_fct_orderline.sql` — Lectric UNION branch at `:740-860`; GP-311 guard and its rollback history at `:125-136`.
- ⚠ **Measurement scripts are in a session scratchpad and are NOT committed.** The numbers above are reproducible only by re-running them. Committing them to a repo evidence folder is an outstanding item.

## Status hygiene

This ticket is **Done** while the connector runs daily and delivery is undecided. Either the remaining work belongs on a new ticket (share/promotion/delivery) or GP-254 should reopen. Flagging rather than changing the status.
