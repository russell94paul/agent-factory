# Mission: marketing-model-reconstruction-v1 — D3 done-ish, D4 next

**Written 2026-09-01.** Supersedes `mission-commander-2026-09-01.md` and `mission-handoff-2026-09-01.md`
for everything about the mission's *state*; their §0 corrections still stand as history.

`next:` **close D3 (Paul's call on a PROVISIONAL verdict), then D4 — falsify the claim that no new
fact is needed.**

---

## 0. Where the work is

```
worktree   .worktrees/mission     branch mission/marketing-model-v1
HEAD       6edb791                (NOT merged to main — main is at 8fba030)
primary    main @ 8fba030         integration surface only; do NOT run mission work there
evidence   docs/evidence/marketing-model-v1/
```

⛔ **Run D4–D5 from the worktree.** The primary checkout invalidated two of this session's own
measurements — see [[F100]]. `repo.data()` resolves to `primary()/.data`, so mission state is shared
and correct from either.

## 1. Mission state — verify, do not trust this table

```
R1 2b9aae3b done · R2 3d053975 done · R3 e397be46 done · D1 1785f5a9 done · D2 933e6c33 done
D3 387780b5 OPEN   ← deliberately not closed
D4 b1f38c1c blocked on D3 · D5 91088e54 blocked on D4
```

```bash
python -c "
from factory.tasks import TaskStore; from factory import repo
st=TaskStore(repo.data()/'tasks.jsonl')
for t in ('387780b5','b1f38c1c','91088e54'): x=st.get(t); print(t,x.status,x.blocked_by)"
```

## 2. ⛔ Why D3 is open, and the one decision that unblocks it

**D3's verdict is `PROVISIONAL` and its recommendation is _no new fact_.** That is a material change
of direction from what the mission assumed, so it was **not** closed unilaterally.

Two things need Paul:
1. **Accept or reject "don't build a fact."** If accepted, D4's job changes from *falsify the
   design* to *falsify the claim that the estate needs no new design*.
2. Nothing else. There is no credential gate, no access gate, no client gate on the path.

4 of keel's 8 acceptance criteria are unmet and named in
`D3-candidate-designs-and-verdict.md` §5 — the (measure × axis) census, the no-bare-zeros rule,
per-key coverage, and a worked reconciliation.

## 3. What is measured and must not be re-derived

| | |
|---|---|
| 8-part grain | **unique AND minimal** — 0 excess; dropping `AD_GROUP_ID`+`PROFILE_ID` → 10,558 excess |
| channel mix | Amazon 901,086 · Google 12,315 · Meta 550 → **98.6% Amazon by row count** |
| data floors | Meta **2026-04-20** · Google 2024-01-01 · Amazon 2024-06-01. Sponsored Display **absent** |
| `ENTITY_CODE` nulls | **0** on every platform — L11 not at risk |
| estate | **84 `MARKETING_*` / 34 names / 7 schemas**; 5 copies of the central fact |
| consumer copy | `WAREHOUSE_TEST_GP226`, **22 cols** (no `COST_USD`) vs `WAREHOUSE`'s 24; 913,951 rows vs 923,517 |
| mixed grain | **19 campaign-days carry both depths** — three live queries `SUM` across both |
| cap | inert, **0 of 5 months** bite |

## 4. Gotchas earned — these cost real time

⛔ **The consumer's Snowflake connection defaults to the clone** (`warehouse.ts:2029`). All six fact
references are unqualified. **Any warehouse change is delivered by a repoint, not a build.**

⛔ **Never `COUNT(DISTINCT a,b,c)` for a grain test** — it drops rows where any expression is NULL
and fabricated 12,504 phantom collisions ([[F101]]). Use `GROUP BY … HAVING COUNT(*) > 1`.

⛔ **And a sentinel is invisible to both halves of that test** ([[F102]]). Every key column needs a
NULL census **and** a distinct-value census. Google/Meta are 100% `PRODUCT_ID='-1'`, two spellings.

⛔ **Never inherit the target.** The grain proof measured an object no consumer reads, because R2
cited four *unqualified* lines as proof of qualification. Three documents, one unchecked claim.

⚠ **Credentials:** `snowflake-admin-nonprod` is **`PAULRUSSELLADMIN`'s**, not
`TEST_DG1_CORE_ADMIN`'s. Password auth is refused account-wide for **MFA**, so service identities
need key-pair. `R3_CARTOGRAPHY` exists, key-pair only, secret
`snowflake-r3-cartography-nonprod` in `aldc-vault-test`. **The wiki vault is not needed and the
classifier blocks reading it.**

⚠ **Bash heredocs mangle backslashes here.** Use Write/Edit for any content with regex or escapes.

⚠ **`pytest -q` prints no summary line in this environment.** Report `collected` and the progress
marks; never infer "N passed". And write `PYTEST_EXIT=$?` on its **own line** — a compound
command's status is not pytest's.

## 5. The one live tool

`scripts/snowflake_bootstrap_r3.py` — pre-authorised by name. Modes:
`--plan` (default, touches nothing) · `--discover` · `--apply` · `--rekey` · `--verify` ·
`--cartograph` · `--grain` · `--probe` (consumer layer) · `--semantics`.
All need `--warehouse COMPUTE_WH`.

## 6. D4's obligations, already written

In `D3-candidate-designs-and-verdict.md` §6. The one people will get wrong:

⭐ **Expect the suite to turn RED.** `warehouse.test.ts:170-177` asserts
`expect(meta.groundedRoas).toBe(0)` under a title claiming it is *"not fabricated"* — it is
`safeDiv(0,0)`. **The suite currently protects the defect; redness is the pass condition.**

## 7. Not done — the honest list

- **D3 not closed.** Verdict PROVISIONAL, awaiting Paul.
- **Jira NOT posted.** Atlassian MCP unavailable. Draft at
  `boot-prompts/drafts/GP-319-comment-2026-09-01.md` — **needs pasting by hand, and the key needs
  confirming** (GP-319 chosen by content match).
- **Branch `mission/marketing-model-v1` is NOT merged and NOT pushed.**
- **No rendered/visual validation anywhere.** Every claim about what a user sees is from reading
  source. The consumer-layer rule in CLAUDE.md is **unsatisfied** for this pass.
- **`Organic Sales Gross`** flagged at `DOCUMENTED` only (June model capture, not re-measured).
  Must be confirmed live before anyone raises it.
- **`paulrussell` password still spans non-prod and prod**; three credentials exposed 2026-08-31,
  rotation unconfirmed. I used that credential today.
- **Jira comment 36056's phantom reason** still uncorrected.
- **`SESSION-COORDINATION-01`** recorded as a candidate at
  `missions/session-coordination-01/00-MISSION-CANDIDATE.md`. **Not launched**, and must not start
  before D5.
- **`F99` credential resolver** — deferred by commander decision, not before D5.
